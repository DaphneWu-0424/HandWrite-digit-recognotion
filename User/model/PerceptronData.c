#include "PerceptronData.h"

/*
 * Placeholder model so the firmware can compile before Python training is run.
 * Replace this file with tools/train/export_perceptron.py output for real use.
 */
const float g_perceptron_weights[PERCEPTRON_CLASS_COUNT][PERCEPTRON_INPUT_SIZE] = {0.0f};
const float g_perceptron_bias[PERCEPTRON_CLASS_COUNT] = {0.0f};
